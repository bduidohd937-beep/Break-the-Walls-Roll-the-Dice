import type { StageDef } from "../game/types";
type Props={stages:StageDef[];unlockedStage:number;clearedStages:number[];devMode:boolean;onSelect:(index:number)=>void};
export function StageSelectPanel(p:Props){return <div className="stage-grid">{p.stages.map(stage=>{const unlocked=p.devMode||stage.id<=p.unlockedStage,cleared=p.clearedStages.includes(stage.id);return <button key={stage.id} className={"stage-card "+(unlocked?"unlocked ":"locked ")+(cleared?"cleared":"")} disabled={!unlocked} onClick={()=>p.onSelect(stage.id-1)}><div className="stage-card-top"><span>STAGE {stage.id} · {stage.region}</span><b>{cleared?"✓ CLEAR":unlocked?"▶ PLAY":"🔒 LOCKED"}</b></div><div className={`stage-type stage-type-${stage.type}`}>{stage.type==="boss"?"BOSS":stage.type==="elite"?"ELITE":"NORMAL"}</div><h2>{stage.name}</h2><div className="stage-card-meta"><span>🌊 {stage.waves.length} WAVES</span><span>🏰 HP {stage.enemyCastleHp}</span></div>{stage.bossName&&<div className="stage-boss-name">👑 {stage.bossName}</div>}{stage.mechanic&&<div className="stage-mechanic">{stage.mechanic}</div>}<div className="stage-card-reward">{cleared?`REPEAT · +${stage.repeatReward} 🪙`:`FIRST · +${stage.clearReward} 🪙 · +${stage.firstClearGems} 💎`}</div></button>})}</div>}